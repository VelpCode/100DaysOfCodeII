


const banner = document.querySelector("#banner");
banner.getAttribute("id");
banner.getAttribute("disabled");

const button = document.querySelector("#login")
button.getAttribute("disabled")


banner.getAttribute(key)
banner.setAttribute(key)
banner.hasAttribute(key)

return button.hasAttribute("disabled")

//complete the enable button function such that it enables the button it recievies 
/* all button id's are set to disabled make it so it's enabled
*/

button.removeAttribute("disabled")

//ths attribute method will remove disabled from all id's so it can be enabled.